# sops-nix encryption rules.
# The `age` recipient below is derived from ~/.ssh/id_ed25519.pub via ssh-to-age.
# Any secret file matching `path_regex` is encrypted to these key(s).
keys:
  - &lenny_laptop age1xcrtfuglw5ulr72w4p9nt30rwqc3kfd25yfxagzvnpus3v9fmqcqr7nlvl
creation_rules:
  - path_regex: secrets/[^/]+\.yaml$
    key_groups:
      - age:
          - *lenny_laptop
